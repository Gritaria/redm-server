# FiveCheck

Aplicativo web para análise de servidores FiveM com foco em sinais de jogadores suspeitos.

## Executar localmente

Pré-requisito: Node.js

1. Instale as dependências:
   `npm install`
2. Rode o app:
   `npm run dev`